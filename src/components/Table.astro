---
import Caption from "./Caption.astro";

interface Props {
  label?: string;
  caption?: string;
}

const { label, caption } = Astro.props;
---

<figure>
  <slot />
  <Caption label={label} caption={caption} />
</figure>
