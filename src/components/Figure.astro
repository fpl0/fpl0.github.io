---
interface Props {
  src: string;
  alt: string;
  label?: string;
  caption?: string;
}

const { src, alt, label, caption } = Astro.props;
---

<figure>
  <img src={src} alt={alt} />
  {
    caption && (
      <figcaption>
        {label && <strong>{label}</strong>}
        <span set:html={caption} />
      </figcaption>
    )
  }
</figure>
