---
interface Props {
  label?: string;
  caption?: string;
}

const { label, caption } = Astro.props;
---

<figure>
  <slot />
  {
    caption && (
      <figcaption>
        {label && <strong>{label}</strong>}
        <span set:html={caption} />
      </figcaption>
    )
  }
</figure>
